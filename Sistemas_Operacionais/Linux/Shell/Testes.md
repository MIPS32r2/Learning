quando precisamos fazer testes no 

\((EXPRESSÕES)) - Para fazer testes  aritméticos usamos

## Operadores numéricos

-eq = verifica se a expressão 1 é igual a expressão 2
-v = verifica se existe a variável
-z = diz que a variável não vai expandir nada
-n = diz que a variável vai expandir